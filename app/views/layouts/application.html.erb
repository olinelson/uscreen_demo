<!DOCTYPE html>
<html data-theme="pastel">
  <%= render "layouts/head" %>
  <body data-controller="plausible" class="min-h-dvh grid grid-rows-[auto_1fr]">
    <nav class="sticky top-0 z-20">
      <%= render "layouts/resources" %>
      <%= render "layouts/nav" %>
    </nav>
    <main class="bg-base-200">
      <%= yield %>
    </main>
  </body>
</html>
