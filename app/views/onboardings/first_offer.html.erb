<div class="container mx-auto p-4">
  <%= render "steps", steps: @steps, current: "first_offer" %>
  <h1 class="text-3xl font-semibold pt-4 pb-2">First Offer</h1>
  <p class="pb-6">Design your first compelling offer to attract customers</p>

  <div class="card bg-base-100 shadow-xl">
    <div class="card-body">
      <%= turbo_frame_tag "offer_form",
      src: new_offer_path({ return_to: onboarding_path }) %>
    </div>
  </div>
</div>
